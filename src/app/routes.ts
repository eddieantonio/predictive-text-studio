/**
 * Maps names to URLs (routes) used in the app.
 *
 * Referring to the names rather than hardcoding the URLs keeps things DRY.
 */
const routes = {
  landingPage: "/",
  projectInfo: "/project/info",
  projectSources: "/project/sources",
};

export default routes;
